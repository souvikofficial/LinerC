Place the user-supplied image here as `demo-avatar.png`.
(Too large binary files cannot be uploaded via the assistant. Add the provided image manually.)

Filename: demo-avatar.png
Path: apps/web/public/demo-avatar.png

After adding the image, restart the dev server or refresh the page to see the favicon and avatar update.